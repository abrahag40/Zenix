/**
 * Route: /reservas-calendario
 *
 * Hidden from the tab bar (registered with `href: null` in _layout.tsx).
 * Pushed via router.push() from the ReservationListScreen header.
 *
 * Inspection-only week view (Research #4 §5.1, §5.2 #6).
 */

import { CalendarWeekScreen } from '../../src/features/reservations/screens/CalendarWeekScreen'

export default CalendarWeekScreen
